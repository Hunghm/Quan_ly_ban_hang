<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quan_ly_ban_hang">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quan_ly_ban_hang">
        <activity android:name=".Activity.XuatActivity"></activity>
        <activity android:name=".Activity.NhapActivity" />
        <activity android:name=".login.login" />
        <activity
            android:name=".Activity.MainActivity"
            android:theme="@style/Theme.Quan_ly_ban_hang.NoActionBar" />
        <activity
            android:name=".Activity.SplashScreem"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".addUser.addUser"></activity>
    </application>

</manifest>